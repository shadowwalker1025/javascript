function fact() {
   // const num = parseInt(prompt('Enter an integer: '));
    let fact = 1 ,num;
    for (i=1;i<=num;i++){
        fact = fact * i;
    }
    console.log(`the factorial of ${num} is ${fact}.`)
}
fact();
